<div class="container">
  <div class="uk-card uk-card-default uk-card-body">
    <h4 class="uk-heading-divider uk-text-uppercase">
      Наша команда
    </h4>
    <div class="uk-child-width-1-3@m uk-child-width-1-2@s" uk-grid>
      {{#if users.length}}
      {{#each users}}
      <div>
        <div class="uk-card uk-card-default">
          <div class="uk-card-media-top">
            <img src="{{img}}" alt="{{firstName}} {{lastName}}">
          </div>
          <div class="uk-card-body">
            <h4 class="uk-card-title">
              <p>
                {{firstName}}<br />{{lastName}}
              </p>
            </h4>
            <hr>
            <div class="uk-grid-small" uk-grid>
              <div class="uk-width-expand" uk-leader>Профессия:</div>
              <div>{{profession}}</div>
            </div>
            <div class="uk-grid-small" uk-grid>
              <div class="uk-width-expand" uk-leader>Пол:</div>
              <div>{{gender}}</div>
            </div>
          </div>
        </div>
      </div>
      {{/each}}
      {{else}}
      <p>В команде пока ни кого нет. <a href="/statement"> Набор открыт!</a></p>
      {{/if}}
    </div>
  </div>
</div>